{
  "private": true,
  "devDependencies": {
    "playwright": "^1.58.2",
    "prettier": "^3.5.3"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "git config core.hooksPath .githooks"
  }
}
